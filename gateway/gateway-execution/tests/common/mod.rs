//! Shared integration-test fixtures. Each test binary compiles this
//! module; helpers unused by a given binary are expected (same pattern as
//! gateway/tests/common).
#![allow(dead_code)]
pub mod engram_stores;
