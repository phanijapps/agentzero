# Review: Ward Agent Doctrine Template

## Pre-execution

- Made persona-template loading lazy so layouts without root `AGENTS.md` remain fluid.
- Fixed the diagnostic contract to stable, bounded, payload-free reason codes.
- Added hard-link, special-file, exact-content, and no-partial-publication coverage.
- Reused one services-layer bounded reader across creation and synthesis.

## Post-implementation

- Added descriptor-relative no-follow Linux seeding after review found a
  symlinked-parent write risk.
- Added portable canonical pre/post confinement and opened/path identity checks.
- Added Windows volume/file-index identity and single-link validation using
  stable `MetadataExt` fields.
- Verified protected orchestration, routing, lifecycle, API, UI, and Ward-tool
  paths are byte-identical to the captured baseline.

RESULT: APPROVED

Clean — ready to commit.
