## Blockers

**1. Rejected dynamic targets leave the parent session pending.** `gateway/gateway-execution/src/invoke/delegation_handler.rs:114`. `handle_delegation` increments `pending_delegations` before dispatch, but the new invalid-target branch returns after `crash_spawn_failure` without the parent error callback or `complete_delegation` path (`gateway/gateway-execution/src/delegation/spawn.rs:84`, `gateway/gateway-execution/src/delegation/spawn.rs:110`, `gateway/gateway-execution/src/delegation/spawn.rs:1505`), so a mismatched or nonexistent capability assignment can fail closed while leaving root paused indefinitely; the current test only calls the helper directly and cannot catch this lifecycle bug (`gateway/gateway-execution/src/delegation/spawn.rs:1819`). Fix: route rejected dynamic assignments through the common failure completion path, decrement pending and send the parent callback, and add a spawn/dispatcher-level test that fails when pending remains nonzero.

**2. Spec metadata still says the implementation is not shipped.** `docs/specs/execution-capabilities/spec.md:3`. The workflow is in final code review with `gates-clean` recorded (`docs/specs/execution-capabilities/engine-state.json:7`), but the spec status is `Approved`, every acceptance criterion remains unchecked starting at `docs/specs/execution-capabilities/spec.md:115`, and the specs index separately says `Implementing` (`docs/specs/README.md:40`), violating the repo spec metadata contract for a shipping implementation. Fix: set the spec and index statuses to the correct shipping state and mark every satisfied AC `[x]` or defer it with a registered backlog anchor.

**3. Required T6 verification evidence is not recorded.** `docs/specs/execution-capabilities/plan.md:391`. T6 requires exact focused-test, formatting, `cargo check -p gateway-execution`, `git diff --check`, gateway full-mode E2E, and Blender/manual-audit results (`docs/specs/execution-capabilities/plan.md:394`, `docs/specs/execution-capabilities/plan.md:397`, `docs/specs/execution-capabilities/plan.md:403`), but the notes directory contains only the resolve/surface disposition and no command-result evidence, while `engine-state.json` only records opaque `gates-clean`; AC-verification, AC-delegated-discovery, and AC-audit-logging cannot be traced to artifacts that would fail if broken. Fix: add the implementation evidence note with exact command/results for each T6 artifact, including the full-mode E2E and Blender/manual QA result or an explicit surfaced unavailability with scope boundary.
