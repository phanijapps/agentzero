## Blockers

**1. Missing archetype transition guide.** `docs/specs/ward-archetype-registry-and-creation/plan.md:116`. The plan and AC11 require `docs/guides/how-to/customize-ward-archetypes.md`, but the guide is still absent, so users have no documented canonical registry paths, singular-template transition, future-ward-only edit semantics, or corrupt-bundle failure behavior. Fix: add the guide or explicitly defer AC11 with a `docs/backlog.md` anchor before shipping.
