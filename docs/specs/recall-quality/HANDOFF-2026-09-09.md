# Session Handoff — 2026-09-09 (op_clean_crap / PR #270)

**Branch:** `op_clean_crap` · **Open PR:** [#270](https://github.com/phanijapps/zbot/pull/270) (21 commits, recall quality + engram piggyback) · **Working tree:** clean, all gates green

## Where things stand

### The recall-quality arc (P0–P3 + Tier D) — COMPLETE

The memory layer is engram end-to-end on the fact/procedure path. Measured on the production path (golden set re-pointed at the engram adapter in D0):

| Metric | sqlite baseline | engram production path |
|---|---|---|
| presence floor | 30/30 | 30/30 |
| precision@5 | 46.7% | **76.7%** |
| correction top-5 | 2/5 | **5/5** |

Key commits (in order): `a6437ef1` P1a adapter fusion · `ef1f85cd` P1b touch_facts · `156ea117` P2 unified on engram RRF (−1,312) · `9eabb320` P3.1 belief ports · `52fd4986` P3.2+3.3 hierarchy/extraction ports · `d8a50aa5` D0 golden-on-engram (+stem matching, +lexical admission parity) · `de7712a0` Tier D sqlite retirement (−5,057, usage-audited).

**Docs:** `docs/specs/recall-quality/analysis.md` (full critical analysis) · `p3-spec.md` (port-slot architecture) · `gateway/gateway-memory/tests/golden_recall/BASELINE.md` (scorecards) · `docs/reviews/2026-09-08/BACKLOG.md` (improvement backlog, mostly consumed)

**Key architecture facts:**
- Adapter fact search = semantic + lexical + temporal lanes fused by engram-retrieval's weighted RRF (`stores/zbot-engram-adapter/src/stores/retrieval_composition.rs`). Temporal lane reorders matched set only, never widens. Tie-aware intake (recency breaks equal-relevance ties).
- Recency half-lives: correction/instruction 180d, user 90d, default 30d, skill/agent exempt. `last_accessed` (touch_facts) refreshes decay — ACT-R usage reinforcement.
- Gateway unified recall uses engram fusion with per-source weights (facts 1.0, procedures 1.1, beliefs 0.9, graph 0.8, wiki 0.9, episodes 0.8, hierarchy 0.7, traversal 0.7, goals 1.2); MMR default-on via engram-rerank-mmr; intent_boost re-sorts lists (was dead, fixed).
- Sleep subsystem: module cores behind engram ports (`BeliefSynthesizer`, `ContradictionDetector`, `HierarchyBuilder`, consolidation executor arms) in `gateway/gateway-memory/src/sleep/{belief_engram,hierarchy_engram}.rs` + extraction arms; orchestration deleted; ConsolidationRequest drives cycles. KG-scoped modules (decay/compactor/pruner/orphan_archiver, conflict_resolver) deliberately NOT migrated (zbot's own KG).
- Golden recall harness: `cargo test -p gateway-memory --test golden_recall -- --ignored --nocapture` — floors are the gate for any further recall work.
- The daemon at `target/debug/zbotd` was rebuilt 2026-09-09 16:05 (includes everything through D0, not Tier D deletion — irrelevant at runtime).

## Session verification (sess-d6737135) — user-confirmed findings

Verified live session "Research AMD vs INTC" on today's build: intent → ward `equity-research` → planner → **two parallel delegations** → continuation → step-3 → respond. All persisted (92 trace events, checkpoints, artifacts). Final distillation success (7 facts, entities, governed relations, procedure `stock_valuation_comparison`, wiki).

**USER CORRECTION (do not re-flag):** the triple distillation is **BY DESIGN** — each subagent delegation completion fires a distill (spawn.rs:1392) so the KG captures discoveries per wave, not at session end. Three waves → three distills → three episodes (partial/partial/success). NOT a bug.

**Open minor items (user saw, no decision yet):**
1. `agent_executions` stuck `running` for root + one builder after session completion (delegation-pause guard skips finalization, nothing backfills). Cosmetic.
2. **No session fork API exists** — the `/fork` ask has no endpoint. Feature would be: copy messages from checkpoint N into a new session (store supports it).

## FINAL (2026-09-10): KG-lane ported, zbot-stores-sqlite DELETED

71da3903: 9 KG-maintenance ops ported to the engram adapter (exact sqlite semantics; one latent sqlite cosine bug fixed — the compactor had been finding nothing); 6 conformance scenarios oracle-verified then pinned; sqlite crate fully deleted (−16,662). NOT ported: 4 episode-attribution ops (list_strategy_candidates etc.) — engram write path records no source_episode_ids (0/2,915 in prod); synthesizer attribution is the prerequisite. Intent tests fixed (0bec9af1). Floors held throughout. Stores layer final shape: traits + domain + conformance + engram-adapter (+ runtime-sqlite/conversation for execution persistence).

## E1+E2 update (late 2026-09-09)

zbot-stores-sqlite reduced to the KG-maintenance reference suite (15,724 → KG-only; wiki/goals/aux/recall-log/kg-episode/bootstrap deleted, −1,483) and the zbot-stores facade deleted (−988, 66 files re-pointed to traits/domain/knowledge-graph::kg_trait). Golden floors held throughout. Two production parity bugs found+fixed via re-point discipline: partial-episode chain drop, layer-limit-0 truncation. Remaining sqlite = intended-but-unported KG-maintenance behavior (porting = the KG-lane decision). OPEN ITEM: intent_analysis_tests (3) fail at HEAD and at every prior commit — stale assertions vs the Sep-7 intent rewrite (injection prose changed); test-update job, not a regression (live sessions route correctly).

## Backlog (remaining, from docs/reviews/2026-09-08/BACKLOG.md + session)

| Item | Size | Notes |
|---|---|---|
| P4 distillation store-phase shrink | ~700 | supersede/dedup phases; low priority, works today |
| KG lane → engram | ~8K | last piggyback chunk; separate decision |
| Gateway shell / AppState (50 fields) decompose | L | biggest structural job in repo |
| `zbot-stores` facade retirement (979-line shim, 63 files) | S–M | mechanical |
| `GraphStorage::run/tx` wrappers (42× ritual in 5,467-line file) | ~S | readability |
| Parallel subagent children | M | `parallel: false` hardcoded at spawn.rs (verify lines — session showed two delegations running concurrently via separate calls, flag may be partially stale) |
| Web search tool | M | SearxNG or similar |
| Shared LLM-JSON parser (3 fence-stripping stacks) | S | do when next structured-output prompt lands |
| Session fork endpoint | S | from verification item 2 |
| Execution-status backfill on session completion | S | from verification item 1 |

## Resume commands

```bash
git status                                    # should be clean
gh pr view 270                                # open PR state
cargo test -p gateway-memory --test golden_recall -- --ignored   # floors gate
cargo clippy --workspace --all-targets --locked -- -D warnings   # clean
```

## Session conventions (established)

- Workers time out at 30m — split large batches; NO giant python heredocs (two hangs were caused by them); use file-scoped edits
- Resume-first on worker timeout (persisted child sessions)
- Commit style: detailed bodies with usage/line-delta reporting
- Engram repo for reference: `~/projects/mem-alpha` (= github.com/phanijapps/engram, main)
- Production data: `~/Documents/zbot/data/` (conversations.db + engram/engram_data.db)
- Daemon: `./target/debug/zbotd --data-dir ~/Documents/zbot`, port 18791
