# Resolve vs. Surface — Execution Capabilities

Opened: 2026-08-04

## Scope

- Resolve in this branch: missing behavior or tests required by the approved
  execution-capabilities acceptance criteria; redaction, fail-closed startup,
  and least-privilege findings inside the existing capability flow.
- Surface: removal of static MCP fallback, persistence or public protocol
  changes, unavailable real Blender/MCP credentials needed for manual QA, or a
  reviewer finding that requires a new product decision.

## Dispositions

- Resolve: exact-ID/display-name alias collision at runtime startup.
- Resolve: host-only catalog provenance, `deleted` diagnostics, and sanitized
  requested-versus-effective assignment logs.
- Resolve: planner step briefing fields and step-executor removal of catalog
  lookup/delegation powers.
- Resolve: missing focused construction tests and AC-to-evidence mapping.
- Resolved after implementation review: all pre-executor failures now use the
  common parent callback, pending-delegation completion, continuation, and
  `wait_agent` rejection path; a full spawn test proves the invalid-target
  branch cannot strand the parent.
- Resolved after security/quality review: `step_executor` suppresses the
  complete same-session agent-control group in addition to planner lookup and
  delegation, with dependency-wired registry coverage.
- Resolved after quality review: capability audit assertions now persist a
  real runtime resolution containing ready, disabled, OAuth-unavailable,
  deleted, and raw-unknown requests, then prove only canonical IDs and closed
  reason codes survive storage.
- Resolved after review: exact automated and real Blender graph evidence is
  recorded in `implementation-evidence.md` and every satisfied AC is anchored.
- Resolved after quality re-review: failures after child-session creation mark
  that child session crashed before releasing the parent continuation; a
  forced missing-provider AgentLoader failure proves no running child is
  orphaned.
- Resolved after final quality review: the loader-failure regression observes
  the synchronous parent-callback boundary and proves the child session is
  already crashed before delegation completion can release continuation.
- Exclude from the final diff: Cargo.lock version churn generated by the
  current workspace's pre-existing manifest/lock mismatch; it is not part of
  this feature.
- Surface separately: enforcing a new persistent grammar for explicit MCP IDs
  would change the shipped MCP ID normalization contract. This branch will
  sanitize bounded display metadata and mark catalog descriptions as untrusted
  reference data, but will not rewrite configured IDs.

Closed: 2026-08-04 — all in-scope review findings resolved; no unresolved
implementation findings remain.
